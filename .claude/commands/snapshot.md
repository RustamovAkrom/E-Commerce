# Update Project Snapshot

Run this command to regenerate PROJECT_SNAPSHOT.md:

```bash
bash scripts/update_claude_snapshot.sh
```

Then read the output and confirm: "Snapshot updated. X Python files, Y TypeScript files found."
Do NOT read the individual files — just confirm the count.
